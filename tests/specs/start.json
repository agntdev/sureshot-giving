[
  {
    "name": "/start replies with the welcome + main menu",
    "steps": [
      {
        "send": { "text": "/start" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "👋 Welcome to SureShot Giving! Tap a button below to get started." }
          }
        ]
      }
    ]
  },
  {
    "name": "Back to menu re-renders the welcome",
    "steps": [
      {
        "send": { "text": "/start" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "👋 Welcome to SureShot Giving! Tap a button below to get started." }
          }
        ]
      },
      {
        "send": { "callback": "menu:main" },
        "expect": [
          { "method": "answerCallbackQuery" },
          {
            "method": "editMessageText",
            "payload": { "text": "👋 Welcome to SureShot Giving! Tap a button below to get started." }
          }
        ]
      }
    ]
  }
]
